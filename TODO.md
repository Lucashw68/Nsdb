# TODO

## 30 October 2025

- Generate schema fields :checkered_flag:
- Generate schema separated from tables :checkered_flag:
- Rename Tables to Models :checkered_flag:
- Update "Clear" sript

## 10 November 2025

- type mapping (e.g. mark required: true using Insert type nullability, detect FKs, enums, etc.)
- Add components (forms, lists, details, etc.)
- Abstract common models functions (empty from schema)
- Simplify usage in components (error handling, loading states, events)
- Why index in schemas and not in models?
- Update README with new architecture