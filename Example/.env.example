# The local playground commands derive Supabase values from `supabase status`.
# Set these only when deliberately using a custom/remote Supabase project.
# SUPABASE_URL=
# SUPABASE_KEY=

# The documentation website runs independently; override this when deployed.
NUXT_PUBLIC_NSDB_DOCS_URL=http://localhost:3001
